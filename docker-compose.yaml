version: "3.7"

services:
  app:
    # build:
    #   context: .
    image: hello-go
    environment:
      DB_HOST: db
    ports:
      - 8080:8080
    depends_on:
      - db
  db:
    image: mongo:4.0.11
    restart: always
